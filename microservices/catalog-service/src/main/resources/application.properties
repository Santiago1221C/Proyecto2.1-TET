spring.application.name=catalog-service
server.port=8080

# Conexión a MySQL Local
spring.datasource.url=${DB_URL:jdbc:mysql://mysql-db:3306/catalogdb?useSSL=false&serverTimezone=UTC}
spring.datasource.username=${DB_USER:bookstore}
spring.datasource.password=${DB_PASSWORD:bookstore123}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html